[project]
name = "agnt5"
version = "0.4.0a2"
description = "AGNT5 Python SDK - Build durable, resilient agent-first applications"
authors = [{name = "AGNT5 Team", email = "team@agnt5.com"}]
license = {text = "Apache-2.0"}
requires-python = ">=3.11"
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Programming Language :: Rust",
    "Operating System :: POSIX :: Linux",
    "Operating System :: MacOS",
    "Operating System :: Microsoft :: Windows",
]
dependencies = [
    "maturin>=1.9.3",
    "docstring-parser>=0.15",
    "typing-extensions>=4.8",
    "httpx>=0.28.1",
    "pydantic>=2.0",
    "sentry-sdk>=2.0.0",
    "edwh-uuid7>=0.2.3",
    "orjson>=3.11.5",
]

[project.urls]
"Homepage" = "https://agnt5.com"
"Documentation" = "https://agnt5.com/sdk/python"


[tool.maturin]
features = ["pyo3/extension-module"]
python-source = "src"
module-name = "agnt5._core"

# Wheel configuration
[tool.maturin.wheel]
classifier = [
    "Operating System :: POSIX :: Linux",
    "Operating System :: MacOS",
    "Operating System :: Microsoft :: Windows",
]

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"


[tool.black]
line-length = 100
target-version = ['py39']

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.9"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
python_files = ["test_*.py", "*_test.py"]
addopts = "--cov=agnt5 --cov-report=html --cov-report=term-missing --cov-report=xml"

[tool.coverage.run]
source = ["src"]
omit = [
    "*/tests/*",
    "*/examples/*",
    "*/__pycache__/*",
    "*/rust-src/*",
    "*/target/*",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "if __name__ == .__main__.:",
    "raise AssertionError",
    "raise NotImplementedError",
    "if 0:",
    "if False:",
]

[dependency-groups]
dev = [
    "coverage>=7.8.2",
    "maturin>=1.8.6",
    "pyright>=1.1.405",
    "pytest>=8.3.5",
    "pytest-asyncio>=1.0.0",
    "pytest-cov>=6.1.1",
    "ruff>=0.11.12",
]

integration = [
    "testcontainers>=4.0.0",
    "psycopg2-binary>=2.9.0",
    "requests>=2.31.0",
]
